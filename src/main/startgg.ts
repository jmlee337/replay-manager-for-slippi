export async function getTournament(key: string, slug: string) {
  return { slug };
}

export async function getEvent(key: string, id: string) {
  return { id };
}

export async function getPhase(key: string, id: string) {
  return { id };
}

export async function getPhaseGroup(key: string, id: string) {
  return { id };
}
